# Storage Schema

| Field | Value |
|---|---|
| Status | Transitional |
| Level | Spec |
| Scope | Subsystem |
| Related | [../runtime/storage-schema.md](../runtime/storage-schema.md) |

## Purpose

This file is a legacy bridge for the older `server/storage-schema.md` path.

The canonical storage/runtime schema description now lives in [../runtime/storage-schema.md](../runtime/storage-schema.md).

## Important Current Correction

The current canonical storage schema should be read through the runtime-oriented storage document, not through older mixed server-design drafts.

In particular:

- local publish does not turn author-local BP preview into published belief snapshots
- storage contracts should be read together with current runtime architecture, not as isolated target-server design

## Migration Note

Use [../runtime/storage-schema.md](../runtime/storage-schema.md) for current work.

This file remains only as a compatibility bridge for older links.
